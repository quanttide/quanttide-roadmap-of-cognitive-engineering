# cli scope

## 范围

思维收集与澄清的命令行入口——用户输入想法，AI 判断清晰度，多轮澄清模糊想法。

## 当前状态

- Rust 实现；analyze/model/repo 模块已归档（`286b86b`），lib.rs 引用断裂
- 旧实现基于已淘汰的模糊模型（意图/想法独立建模）

## 待办：简化

- **对齐两层建模**：收敛为心智模型本体 + 情境意识实例层，移除意图/想法独立模型
- 只保留核心：思维收集（输入）→ 清晰度判断 → 澄清对话
- 精简依赖与命令面（切到 crates.io 发布后简化维护面）
